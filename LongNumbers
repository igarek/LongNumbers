#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
#include<iostream>

using namespase std;
class LongNumbers
{
      protected:
char * number;
int length;

      public:
        LongNumbers();// constructors
        LongNumbers(int deg, double * mas);
        LongNumbers(const LongNumbers &x);
        ~LongNumbers();// destructor
        int getdeg();
        void setdeg(int newdeg);
        double getmas();
        void setmas(double newmas);
       
}

LongNumbers
{
char * number;
int length;
public:
IntNumbers(char * numb)
{
len=strlen(numb);
number=new char[len];
strncpy(number,numb,len);//??
} 

;LongNumbers::LongNumbers()// constructors
        {
          deg=new char [100];
        }
LongNumbers::LongNumbers(char * newdeg, double * newmas)
        {
          int len=strlen(newdeg);
          deg=new char [deg+1];
          strncpy(deg,newdeg,len);
          deg[len+1]=0;
          deg=newdeg;
         
        }
      
